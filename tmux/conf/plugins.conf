# ============================================================================
# Tmux Plugins - TPM Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# Theme Plugins (currently using powerline instead)
# ----------------------------------------------------------------------------
# Catppuccin theme configuration (commented out)
# set -g @catppuccin_flavor 'frappe'
# set -g @catppuccin_window_status_style "rounded"
# set -g @catppuccin_date_time_text "%e %b %H:%M"
# set -g @catppuccin_window_text " #W"
# set -g @catppuccin_window_current_text " #W"
# set -g @plugin 'catppuccin/tmux'

# ----------------------------------------------------------------------------
# Active Theme
# ----------------------------------------------------------------------------
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'erikw/tmux-powerline'

# ----------------------------------------------------------------------------
# Core Plugins
# ----------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# ----------------------------------------------------------------------------
# Session Management Plugins
# ----------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# ----------------------------------------------------------------------------
# Productivity Plugins
# ----------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'wfxr/tmux-fzf-url'

# ----------------------------------------------------------------------------
# Status Bar Plugins
# ----------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'

# ============================================================================
# Plugin Configurations
# ============================================================================

# Resurrect Settings
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'
set -g @resurrect-dir '~/.tmux/resurrect'
set -g @resurrect-strategy-nvim 'session'

# Continuum Settings
set -g @continuum-save-interval '15'
set -g @continuum-restore 'on'

# Yank Settings
set -g @yank_selection 'primary'
set -g @yank_selection_mouse 'clipboard'

# FZF URL Settings
set -g @fzf-url-fzf-options '-p 60%,30% --prompt="  " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'

# ============================================================================
# Initialize TPM (keep at bottom)
# ============================================================================
run '~/.tmux/plugins/tpm/tpm'

# Status bar customization (commented - using powerline)
# set -g status-left-length 100
# set -g status-right-length 100
# set -g status-left "#{E:@catppuccin_status_session}"
# set -gF status-right "#{E:@catppuccin_status_cpu}"
# set -g status-right '#{cpu_bg_color} CPU: #{cpu_icon} #{cpu_percentage} | %a %h-%d %H:%M '
# set -ag status-right "#{E:@catppuccin_status_date_time}"
# set -agF status-right "#{E:@catppuccin_status_battery}"
# run ~/.tmux/plugins/tmux-cpu/cpu.tmux
# run ~/.tmux/plugins/tmux-battery/battery.tmux
